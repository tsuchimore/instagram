<div class="container">
<h2>お知らせ</h2>
<ul style="list-style:none;">
  <% @notifications.each do |notification| %>
  <hr>
    <li>
      <% if notification.read == false %>
      <span>未読</span>
      <% else %>
      <span>既読</span>
      <p>
      <% end %>
        <%= notification.comment.user.try(:name) %>さんが
        あなたのブログ（<%= link_to "#{notification.comment.blog.content}", blog_path(notification.comment.blog, notification_id: notification.id) %>）にコメントしました。
      </p>
      <p><%= notification.created_at %></p>
    </li>
  <% end %>
</ul>
</div>